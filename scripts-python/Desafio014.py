#Conversão de Temperaturas

temperatura = float(input('Qual a temperatura em °C? '))
formula = ((9 * temperatura) / 5) + 32
print('A temperatura de {}°C corresponde a {}°F'.format(temperatura, formula))